{{> header}}
<h1>{{product.title}}</h1>
<p>{{product._id}}</p>
<p>{{product.description}}</p>
<p>Price: ${{product.price}}</p>
<img src="{{#if product.thumbnails.[0]}}{{product.thumbnails.[0]}}{{else}}no_image.jpg{{/if}}" alt="{{product.name}}" style="width: 180px; height: 200px;">

<form id="remove-product-form">
    <input type="hidden" id="remove-product-code" placeholder="Código del producto a eliminar" value="{{product._id}}">
    <button type="submit">Eliminar Producto</button>
</form>

<a href="/products">Back to Products</a>


<script src="/socket.io/socket.io.js"></script>
<script src="/js/index.js"></script>